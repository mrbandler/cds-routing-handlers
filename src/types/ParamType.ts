/**
 * Parameter type.
 *
 * @export
 * @enum {number}
 */
export enum ParamType {
    Srv,
    Req,
    Data,
    ParamObj,
    Param,
    Jwt,
    Entities,
    Next,
    Locale,
}
